package asset.map{		import classes.asset.SimpleButton;	import classes.asset.check.CheckView;	import classes.asset.land.Africa;	import classes.asset.land.Asia;	import classes.asset.land.Australia;	import classes.asset.land.Europe;	import classes.asset.land.NorthAmerica;	import classes.asset.land.SouthAmerica;		import flash.display.DisplayObject;	import flash.display.MovieClip;	import flash.display.Sprite;	import flash.events.Event;	import flash.events.MouseEvent;	import flash.geom.Matrix;	import flash.geom.Point;
	
		public class MapView extends Sprite	{		public var header:MovieClip;		public var map:JPPScrollbarKit;		public var content:MovieClip;		public var northAmerica:NorthAmerica;		public var southAmerica:SouthAmerica;		public var europe:Europe;		public var africa:Africa;		public var asia:Asia;		public var australia:Australia;				private var optionButton:SimpleButton;							public function MapView()		{			super();			this.addEventListener(Event.REMOVED_FROM_STAGE, rFSH, false, 0, true);			header.field.text = "MAP GAME";			optionButton = new SimpleButton("", "work", 0.2, 0.4);			optionButton.name = "optionButton";			optionButton.x = 10;			optionButton.y = 2;			addChild(optionButton);		}				protected function rFSH(event:Event):void
		{			this.removeEventListener(Event.REMOVED_FROM_STAGE, rFSH);
			while(numChildren > 0){				var display:DisplayObject = getChildAt(0);				//trace(display);				removeChild(display);				display = null;			}
		}	}}